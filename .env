# DockerVerse Environment Configuration
# Format: id:name:address:local/remote (pipe-separated for multiple hosts)
DOCKER_HOSTS=raspi1:Raspeberry Main:unix:///var/run/docker.sock:local|raspi2:Raspberry Secondary:http://192.168.1.146:2375:remote

# Watchtower integration
WATCHTOWER_TOKEN=dockerverse-watchtower-2026
# Note: raspi1 uses the Docker bridge gateway IP to access Watchtower on the same host
# raspi2 uses the external IP since it's on a different host
WATCHTOWER_URLS=raspi1:http://172.26.0.1:8080|raspi2:http://192.168.1.146:8081

# Security
JWT_SECRET=dockerverse-super-secret-key-2026

# Frontend
ORIGIN=https://docker-connect.nerdslabs.com
TZ=America/Mexico_City
